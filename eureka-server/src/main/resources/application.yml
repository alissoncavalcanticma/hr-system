spring:
  application:
    name: eureka-server
server:
  port: ${PORT:8761}
eureka:
  instance:
    lease-renewal-interval-in-seconds: 0
  client:
    register-with-eureka: false
    fetch-registry: false